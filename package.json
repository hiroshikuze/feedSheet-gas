{
  "name": "feedsheet-gas-tests",
  "version": "1.0.0",
  "description": "Test suite for feedSheet-gas (pure-function unit tests only; GAS runtime code is not executed)",
  "scripts": {
    "test": "jest --colors"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFiles": [
      "./test/setup.js"
    ]
  }
}
